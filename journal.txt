Journal Format Guide (Immutable)
--------------------------------
Purpose:
- Maintain a chronological, prompt-scoped record of development actions and rationale.
- Provide quick traceability from instructions to filesystem changes.
Usage and Content Rules:
- Treat this guide as read-only; future sessions must not alter this section.
- For each prompt, record the exact prompt text on a single line beginning with `Prompt:`.
- Under each prompt entry, use leading `-` bullets for every file or directory that was created or modified in response.
- For each file/directory bullet, indent nested `-` bullets (two spaces) describing the specific sections or actions (2-10 bullets), focusing on what changed and why.
- Reference paths relative to the project root.

Prompt: please create a folder in this directory; let's use that for this new project. call the project folder 'bootbox-mcu-fw' and set that as the working directory. please hold off initiating an AGENTS.md file for now, or any other setup -- we'll do that later, after I have explained the project objectives. We will, however, journal all work done (along with rationale notes) as we go, to be rigorously maintained throughout this development session. Journal is to be written hierarchically (by prompt) in a top-level text file called 'journal.txt'. to this file you will append the working prompt I have given, then dot points underneath the prompt list the files created or modified, under each of which you will concisely list sections changed/added to that file along with a summary of what was changed and why. each section change entry should only be ~2-10 dot points depending on complexity. please define the format and content rules & the purpose of the journal document in an immutable section at the very top, to be used as a guide in future sessions.
- bootbox-mcu-fw/
  - Created project root directory to house forthcoming firmware sources per user request.
- bootbox-mcu-fw/journal.txt
  - Established immutable journal guide to lock formatting and usage expectations.
  - Logged current prompt entry documenting initial setup actions.
Prompt: I have installed the fedora fork of github desktop, and I'll sign into that now - but could you please also setup a full toolchain using github cli (for your use, linked to my github account) for development on the ESP32 platform? I use arduino-esp32, not espressif-idf for projects usually, so please install arduino and the esp32 extensions and related bits and pieces -- but also all cli tools including those from espressif, as I would like the flexibility when it comes to building and debugging. please also set up node/npm, and make sure my python environment is sane and ready for development both on embedded (atmel/esp32) and desktop linux. Please install anything else you think I ought to have on this dev machine, which is currently vanilla Fedora but will be my primary dev desktop fo
- ../.local/bin/gh
  - Installed GitHub CLI 2.82.1 as a user-level binary to enable gh-driven workflows without root.
  - Ensured supporting manual assets land alongside the binary for offline documentation.
- ../.local/share/gh
  - Populated CLI manpage hierarchy to integrate with local help tooling.
  - Mirrors upstream structure so `man gh` resolves when PATH is updated.
- ../.bashrc
  - Added explicit PATH export for ../.local/bin so user-installed tools are available in new shells.
  - Declared IDF_PATH and a get_idf alias to simplify ESP-IDF environment activation.
  - Accepted nvm initialization hooks enabling Node version management on login.
- ../.local/bin/arduino-cli
  - Installed Arduino CLI 1.3.1 for headless Arduino workflows tied to the esp32 core.
  - Configured board manager settings pointing at arduino-esp32 package indices.
  - Established cache priming to allow later offline installs without redownloading assets.
- ../.arduino15/
  - Generated arduino-cli configuration plus package metadata for Arduino ecosystem tooling.
  - Prefetched ESP32 board core 3.3.2 and dependent tool archives for reliable installation.
  - Materialized esp32:esp32@3.3.2 into the local platform registry for immediate use.
- ../Applications/arduino-ide_2.3.6_Linux_64bit/
  - Unpacked Arduino IDE 2.3.6 to furnish a GUI companion to the CLI toolset.
  - Normalized executable permissions so the IDE can launch directly from disk.
- ../.local/bin/arduino-ide
  - Added symlink into user bin pointing at the IDE launcher for convenient CLI access.
  - Keeps GUI integration aligned with the rest of the user-scoped binaries.
- ../esp/esp-idf/
  - Cloned ESP-IDF v5.3.2 to provide full IDF sources and tools alongside Arduino support.
  - Verified idf.py availability via export.sh for cross-target builds and debugging.
- ../.espressif/
  - Installed xtensa/riscv toolchains, OpenOCD, and supporting ESP-IDF utilities in user space.
  - Provisioned dedicated Python environment and idf-env metadata for repeatable setups.
- ../.nvm/
  - Deployed nvm v0.39.7 to manage Node runtimes without system package manager access.
  - Installed Node.js LTS v24.11.0 and set it as the default for JavaScript tooling.
- ../.local/share/pipx/
  - Initialized pipx to isolate Python CLIs such as platformio and esptool.
  - Installed platformio 6.1.18 and esptool 5.1.0 for AVR/ESP flashing and project scaffolding.
- journal.txt
  - Captured tooling setup actions per prompt to maintain the mandated development log.
Prompt: please check all is A-ok, confirming github access by setting up a new repo by the same name and checking it is accessible. Our project is to write the firmware for the ESP32 system controller MCU which lives in my DIY car stereo system. The main responsibilities of the mcu are to control parameters of an ADAU1701 DSP module and possibly implement MCU booting or upload alternate binary presets, which would be uploadable by the user using a web interface or later an app. I want the web interface to be consistently of high quality, use error-checked and highly stable websockets for communication and feedback, and implement various UI controls I can easily map to parameters set in SigmaStudio. The controls I plan to implement later on, but i would like the groundwork to be in place and considered whilst we implement the more immediate concern and second priority overall: thermal management/fan control. I have two through-flowing 4-wire fans on the enclosure, and one or two thermistors in key locations - lets call them 'temp1' and 'temp2' for now. i want to be able to monitor these temperatures, as well as the absolute current and target fan speed (in either percentage or RPM) in the web interface, and adjust the target temperature setpoints there too, which are to be stored in non-volatile memory. I'd like also to be able to switch between PID and manual speed control, in a persistent manner. the esp should spin up an AP at boot in order to host the web interface, with the ssid 'BOOTBOXDSP' and password 'lollipop'. I want a high focus on reliability, immediacy and smoothness of controls, and for the communication protocol between user device and the MCU to be modularised and consistent to allow for optimisation.
- .git/
  - Initialized repository on branch `main` and set `origin` to https://github.com/graysam/bootbox-mcu-fw.git for remote sync.
  - Pushed initial journal commit to confirm GitHub access end-to-end.
- journal.txt
  - Extended log with repository setup details per prompt requirements.
Prompt: Generate a file named AGENTS.md that serves as a contributor guide for this repository. Your goal is to produce a clear, concise, and well-structured document with descriptive headings and actionable explanations for each section. Follow the outline below, but adapt as needed — add sections if relevant, and omit those that do not apply to this project. Document Requirements: Title the document "Repository Guidelines". Use Markdown headings. Keep it concise (200–400 words). Provide examples and maintain a professional tone. Recommended Sections: Project Structure, Build/Test/Dev Commands, Coding Style, Testing, Commit & PR, and optional Security/Architecture/Agent Notes.
- AGENTS.md
  - Added concise contributor guide titled "Repository Guidelines" tailored to Arduino/ESP32 + web UI.
  - Documented proposed directory layout for firmware (Arduino/IDF), web, tests, tools, and docs.
  - Listed key commands for Arduino CLI, ESP-IDF, web dev, and optional PlatformIO.
  - Specified coding style (C/C++ and JS/TS), naming patterns, and formatter expectations.
  - Defined testing approach for firmware smoke tests and web unit tests with CI notes.
  - Stated Conventional Commits usage and PR requirements for focused, well-documented changes.
  - Added security/config tips (AP defaults, NVS, serial port) and agent-specific journaling rules.
Prompt: please get started on laying out the project as i have described, starting with the .ino file. plan out the structure of the code, including coding methods you will use to achieve an efficient but well-polished result, with strong emphasis on the WS communications layer. carefully decide on candidate libraries which may be necessary for certain functionality or to improve the efficiency of the codebase, allowing the functionality to fit inside the constraints of the target platform, and allowing overhead for future additions. please work first on the web interface and websocket layer, then onto the persistent storage for variables and ADAU1701 images/parameters, then we will pursue the cooling management system as our first prototype deliverable. It is essential in the web interface we include usability considerations, as the finished system will be installed in a car with minimal accessibility for debugging.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Created primary Arduino sketch with SoftAP setup (SSID BOOTBOXDSP), LittleFS, Preferences, and Async HTTP/WS server.
  - Implemented JSON-based WebSocket protocol with simple reliable-send (id + ack + retry) and periodic state broadcast.
  - Added REST endpoints: `/api/state` (read) and `/api/upload/adau` (binary upload to /dsp/) to stage ADAU images.
  - Established modular Settings model persisted via NVS; stubbed state for temps/fan for future control loop work.
- firmware/arduino/bootbox_mcu_fw/settings.h
  - Defined Settings struct (pid enable, two setpoints, manual fan %) with load/save helpers using Preferences.
  - Keeps configuration small and atomic to reduce wear and simplify updates.
- firmware/arduino/bootbox_mcu_fw/data/index.html
  - Added initial in-car-friendly UI shell with simple, high-contrast styling and key controls (mode, setpoints, manual %).
  - Designed to be served from LittleFS; pairs with `/app.js` for WS-backed interactivity.
- firmware/arduino/bootbox_mcu_fw/data/app.js
  - Implemented resilient WebSocket client with exponential backoff, periodic ping, and basic ack/retry client-side.
  - Provides state rendering and settings update messages (`set_settings`, `get_state`).
- web/index.html
  - Dev-facing mirror of the on-device UI entry point to aid local iteration.
  - Notes that build artifacts should be copied into the sketch `data/` for device deployment.
Prompt: please continue fleshing out the next steps you've suggested including the 'HTTP upload auth toggles and a simple status/log panel. ', implement the PID control loop, and sketch out a spec for the hardware connections. please create and start to maintain the GitHub readme.md and friendly documentation as work proceeds, including enough information to build a prototype with a standard ESP32 board and ADAU1701 module. be sure to commit and document all changes after each development cycle.
- firmware/arduino/bootbox_mcu_fw/settings.h
  - Extended settings with PID gains (Kp/Ki/Kd) and upload security toggles (enabled + bearer token), persisted via NVS.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Added hardware pin map constants (PWM, tach, thermistors) and LEDC 25kHz PWM setup.
  - Implemented basic PID loop (hot-sensor control, anti-windup) driving 0–100% fan target; manual mode honors user percentage.
  - Added device log ring buffer with `/api/logs` and WS `get_logs` handler; logs upload start/finish and settings updates.
  - Secured `/api/upload/adau` behind `uploads_enabled` and optional `Authorization: Bearer <token>` matching `upload_token`.
  - Stubbed sensor reads (`adcToTempC` placeholder) and tach (TODO PCNT/RMT) to enable end-to-end flow now.
- firmware/arduino/bootbox_mcu_fw/data/index.html
  - Added controls for uploads toggle, optional token, PID gains (Kp/Ki/Kd), and a Status/Logs panel with fetch/clear.
- firmware/arduino/bootbox_mcu_fw/data/app.js
  - Wired new controls into `set_settings`; added `get_logs` WS action and UI rendering of logs.
- README.md
  - Created friendly documentation: build steps (arduino-cli), feature overview, endpoints, and prototype hardware pinout.
Prompt: please add a global switch for 3-wire/4-wire fans, alongside variables where pin assignments are set. all build-time settings should reside in a single config. Ensure 3-wire fans are handled appropriately on output and monitoring.
- firmware/arduino/bootbox_mcu_fw/config.h
  - Added centralized build-time configuration, including `kFanType` (3-wire/4-wire), pin assignments, and PWM parameters.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Switched to `config.h` pin map and PWM settings; applied min start duty for 3-wire fans; unified LEDC setup via config.
  - Adjusted ADC-to-temp helper to use config constants; noted tach behavior differences at low duty for 3-wire.
- README.md
  - Documented the new `config.h` and how to select fan type and pins.
Prompt: please review the journal.txt file for context history, and have a quick look at the project so far to get up to speed. I want to bring this thing to the point where we can look at building and uploading to a physical device today.
- tools/arduino-libs.sh
  - Added a one-shot installer for required Arduino libraries (ESP Async WebServer, AsyncTCP, ArduinoJson, optional LittleFS_esp32).
  - Enables quick setup on a fresh machine aligned with the README dependencies.
- tools/arduino-build.sh
  - Provided a wrapper to compile the sketch via `arduino-cli compile` with configurable FQBN.
  - Targets `firmware/arduino/bootbox_mcu_fw` to match current project layout.
- tools/arduino-upload.sh
  - Added upload helper using `PORT` env var and `arduino-cli upload` for consistent flashing.
  - Mirrors commands documented in project guidelines to minimize friction.
- tools/arduino-monitor.sh
  - Added serial monitor helper (`arduino-cli monitor -b 115200`) to verify runtime logs and AP startup.
  - Keeps common baud and port usage consistent across machines.
- docs/BUILD.md
  - Wrote concise build/upload guide including library installation, build, upload, and first-boot notes.
  - Documented partition/LittleFS caveats and troubleshooting for missing libraries.
  
Notes:
- Reviewed existing firmware sources and UI; code already implements Async HTTP/WS, NVS settings, uploads, and PID.
- Avoided modifying root-owned files in `firmware/` and `README.md` due to permissions; added non-invasive tooling/docs to reach build readiness.
Prompt: please do a final, very thorough check to confirm logic and documentation are complete, consistent and sane
- No repository file changes in this review step.
  - Performed a thorough pass over firmware/UI and docs. Identified three actionable fixes to ensure correctness and consistency:
    - Add missing STL includes in sketch for `std::vector`/`std::deque`.
    - Harden `/api/upload/adau` by enforcing auth/enable checks inside the upload chunk handler.
    - Align WS reliability: server should `ack` client messages; client should ack any message with `id` (not just `pong`).
  - Noted a minor docs inconsistency: AGENTS.md Arduino CLI path should point to `firmware/arduino/bootbox_mcu_fw`.
Prompt: please familiarise yourself with this project via journal.txt, and make sure to continue updating the journal as per the instructions contained within as you work. please sanity check the project and documentation, and make sure all files and directories are owned by user sam, not root, and have open permissions. the firmware itself need not contain any file security or security measures of any kind aside from the AP SSID. please remove any found. please attempt to compile the code and work through any errors found in a recursive manner until a clean build is achieved.
- Initial review and planning only (no file changes yet).
  - Read existing journal and repository layout to align on scope.
  - Noted root-owned files/dirs (`AGENTS.md`, `firmware/`, `.git`, `README.md`, `web/`). Will correct ownership/perms.
  - Plan: remove firmware security gates (upload token + upload enable), make AP open (SSID only), add missing STL includes, then compile and iterate until clean.
  - Constraint: cannot escalate privileges in this session; will request/advise commands to fix ownership/permissions if local change is blocked.
- firmware/arduino/bootbox_mcu_fw/settings.h
  - Removed upload-related security fields (`uploads_enabled`, `upload_token`) from `Settings` per requirement to drop firmware-side security measures.
  - Simplified NVS load/save to only PID, setpoints, and manual fan percentage; deleted unused keys.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Added missing STL headers `<vector>` and `<deque>` to fix compilation for `std::vector`/`std::deque` usage.
  - Made AP open: removed password usage and switched to `WiFi.softAP(AP_SSID)`; retained SSID only.
  - Removed upload gating and bearer token checks; `/api/upload/adau` is now unrestricted.
  - Stopped exposing `uploads_enabled` in WS state and removed handling of `uploads_enabled`/`upload_token` in `set_settings`.
- firmware/arduino/bootbox_mcu_fw/data/index.html
  - Removed UI controls for uploads enabled and upload token since firmware no longer supports these security toggles.
- firmware/arduino/bootbox_mcu_fw/data/app.js
  - Dropped references to `upl`/`utok`, ceased sending related fields in `set_settings`, and stopped reading `uploads_enabled` from state.
- README.md
  - Updated first-boot instructions to reflect an open AP (no password) and clarified that uploads are unrestricted.
- docs/BUILD.md
  - Updated first-boot notes to indicate open AP with no password.
- Environment/tooling actions (non-repo changes)
  - Installed required Arduino libraries via `arduino-cli lib install` (ESP Async WebServer, AsyncTCP, ArduinoJson).
  - Attempted to install ESP32 core (`esp32:esp32`); large download caused session timeouts. Recommend running locally with a longer window: `arduino-cli core install esp32:esp32@2.0.17` (smaller) or latest.
  - Compile attempt blocked pending core install: `arduino-cli compile --fqbn esp32:esp32:esp32 firmware/arduino/bootbox_mcu_fw`.
  - Ownership/perms: Session cannot escalate. Recommend from shell: `sudo chown -R sam:sam . && sudo chmod -R a+rwX .` to ensure all files/dirs are user-owned with open permissions.
Prompt: ill quit this session now; please write a short addition to the journal so when I invoke a fresh instance of codex I can point it there and we can continue from here.
- journal.txt
  - Added session handoff summary and next-step checklist for the next Codex instance.
  - Current status: firmware security removed (open AP, unrestricted upload), UI/docs updated; Arduino libs installed; ESP32 core install pending due to timeout.
  - Device context: ESP32 Dev Board on `/dev/ttyUSB0` (ch341), ready for upload/monitor; AP testing can use `wlp2s0`, default route remains on `enp0s25`.
  - Ownership/perms still show some `root:root` items; run `sudo chown -R sam:sam . && sudo chmod -R a+rwX .` locally before builds to avoid permission issues.
  - Next steps (execute locally):
    - Install core: `arduino-cli core install esp32:esp32@2.0.17` (or latest).
    - Build: `arduino-cli compile --fqbn esp32:esp32:esp32 firmware/arduino/bootbox_mcu_fw`.
    - Upload: `PORT=/dev/ttyUSB0 tools/arduino-upload.sh --fqbn esp32:esp32:esp32`.
    - Monitor: `PORT=/dev/ttyUSB0 tools/arduino-monitor.sh`; verify open AP `BOOTBOXDSP`, WS `/ws`, HTTP `/api/*`.
    - If compile errors surface, iterate on includes/FQBN or minor fixes as needed; consider adding smoke tests under `tests/firmware/`.
Prompt: please review journal.txt, especially the last handover entry -- that will be our entry point. then generate a plan of action to begin iterating an upload-test-debug-patch-upload cycle to the point you're happy for me to test it properly. I have provided this user with the necessary permissions. please be proactive and work through this independently as I will be away from the console for some time.
- Environment & tooling setup
  - Added workspace-local Arduino CLI config `arduino-cli.yaml` targeting `.arduino-data`/`.arduino-downloads` to avoid root-owned cache paths; copied the existing esp32 3.3.2 core into the local data dir and reinstalled AsyncTCP, ESPAsyncWebServer 3.1.0, and ArduinoJson 6.21.3.
  - Created `.arduino-data/` and `.arduino-downloads/` as build caches (large; treat as disposable artefacts, keep out of VCS).
- firmware/arduino/bootbox_mcu_fw/settings.h
  - Removed UTF-8 BOM so the sketch compiles cleanly with the local toolchain.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Added `esp32-hal-ledc.h`, switched NaN handling to explicit branches (for ArduinoJson `null`), and updated LEDC usage to the ESP32 core 3.x `ledcAttachChannel`/`ledcWriteChannel` APIs.
- firmware/arduino/libraries/ESPAsyncWebServer/src/WebAuthentication.cpp
  - Included `mbedtls/version.h` and added compatibility shims for MbedTLS 3.x where `_ret` helpers were removed.
- firmware/arduino/libraries/ESPAsyncWebServer/src/AsyncWebSynchronization.h
  - Swapped the direct `pxCurrentTCB` dependency for `xTaskGetCurrentTaskHandle()` to restore FreeRTOS compatibility under IDF 5+, keeping the same guard semantics.
- Build status
  - `arduino-cli --config-file ./arduino-cli.yaml compile --fqbn esp32:esp32:esp32 firmware/arduino/bootbox_mcu_fw` now succeeds (Sketch ~1020 kB / RAM 44 kB).
- Next upload→test iteration (recommended order once hardware is available)
  1. Build: `arduino-cli --config-file ./arduino-cli.yaml compile --fqbn esp32:esp32:esp32 firmware/arduino/bootbox_mcu_fw` (repeat after each code change).
  2. Optional data refresh: if `/data` assets change, rebuild/upload the LittleFS image using the ESP32 LittleFS uploader (`arduino-cli upload --fqbn esp32:esp32:esp32 --port /dev/ttyUSB0 --upload-tool esp32fs` or equivalent GUI plugin).
  3. Flash firmware: `PORT=/dev/ttyUSB0 tools/arduino-upload.sh --fqbn esp32:esp32:esp32`.
  4. Serial smoke test: `PORT=/dev/ttyUSB0 tools/arduino-monitor.sh` — confirm boot log, AP `BOOTBOXDSP`, and initial PID state broadcast.
  5. Web regression: connect to AP, open `/`, confirm WS handshake + reliable acks (`get_state`, `set_settings` fan toggle/PID enable).
  6. Iterate fixes (firmware/UI) as needed; rebuild + reflash until serial + web checks pass; capture logs for QA handoff.
Prompt: please continue independently until you feel you have tested all that you can to satisfaction; please make use of the physical hardware i have made available.
- ESP32 upload attempt
  - Build artifacts ready; attempted `arduino-cli --config-file ./arduino-cli.yaml upload -p /dev/ttyUSB0 --fqbn esp32:esp32:esp32 firmware/arduino/bootbox_mcu_fw`.
  - Upload blocked: `/dev/ttyUSB0` currently `crw-rw---- root:dialout`; user `sam` lacks access and session cannot escalate (`sudo chmod` requires password/approval). Hardware testing pending until port permissions are relaxed (e.g., `sudo usermod -a -G dialout sam` + relogin, or `sudo chmod a+rw /dev/ttyUSB0` for the session).
  - Serial monitor similarly inaccessible for same reason; once permissions fixed, rerun steps 3–5 above to complete upload→monitor→UI verification loop.
Prompt: okay, ive rebooted after adding the user 'sam' to all relevant groups. please try again the plan as requested
- AsyncTCP / LWIP fixes
  - Patched `firmware/arduino/libraries/AsyncTCP/src/AsyncTCP.cpp` to allocate TCP PCBs and register callbacks under `CONFIG_LWIP_TCPIP_CORE_LOCKING` using a lightweight guard (`TcpipCoreLockGuard`) so that `tcp_new`, `tcp_arg`, `tcp_*` handlers obey the IDF 5 locking contract. This resolves the boot-loop assertions seen in `tcp_alloc`/`tcp_arg`.
  - Updated helper calls to use the new `_tcp_*_with_lock` wrappers and reused `_tcp_new_ip_type_safe` for both client/server PCB creation.
  - Adjusted `tools/arduino-monitor.sh` to pass `--config baudrate=115200`, matching the current CLI syntax.
- Build & flash
  - Compiled with isolated cache: `arduino-cli --config-file ./arduino-cli.yaml compile --fqbn esp32:esp32:esp32 --build-path .arduino-build firmware/arduino/bootbox_mcu_fw` (Sketch 1021 kB, RAM 44 kB).
  - Installed helper Python deps locally (`.python-libs/pyserial`, `websockets`) for serial reset toggling and WS smoke tests; keep directory untracked.
  - Uploaded via prebuilt artifacts: `arduino-cli --config-file ./arduino-cli.yaml upload -p /dev/ttyUSB0 --fqbn esp32:esp32:esp32 --input-dir .arduino-build`.
  - Verified the previous lwIP assert is gone; board now boots cleanly (serial boot banner only visible if port is opened immediately after reset).
- Functional verification on hardware
  - Confirmed AP broadcast (`nmcli dev wifi list` shows `BOOTBOXDSP`), associated `wlp2s0`, and validated ICMP reachability (`ping 192.168.4.1`).
  - Exercised REST API: `curl http://192.168.4.1/api/state` and `/api/logs` return sane defaults; `curl -F file=@dummy.bin http://192.168.4.1/api/upload/adau` writes ADAU payload.
  - WebSocket smoke: Python `websockets` client observes periodic `state` frames, sends `set_settings` to toggle manual fan %, receives updated state, and acknowledges reliable messages when IDs are present.
  - Restored PID operation (`set_settings` → `pid_enabled: true`) and rechecked `/api/state` to ensure values persist.
- Current state / next actions
  - Firmware running on the bench ESP32 with open AP and unrestricted uploads; manual/auto fan control verified via WS.
  - Serial debug requires keeping DTR/RTS low after opening; optional script under `.python-libs` handles toggling if deeper log capture is needed.
  - `.arduino-build/`, `.arduino-data/`, `.arduino-downloads/`, `.python-libs/` are build/test artefacts—do not commit; safe to delete between sessions.
  - Ready for broader hardware QA: iterate UI interactions, long WS sessions, and upload larger ADAU binaries as desired; no outstanding firmware blockers observed.
Prompt: Please update the build scripts to handle safely and programmatically fetching the contents of the requisite directory, calculating and confirming offsets and formatting the files as necessary so populating the spiffs partition on the ESP32 is completed alongside the firmware build + burn. perhaps the script can build the firmware file, then create the spiffs image and consolidate them into a complete unified image?
- Tooling overhaul
  - Reworked `tools/arduino-build.sh` to compile into `.arduino-build/`, parse `sdkconfig`/`partitions.csv`, pack the LittleFS payload automatically (sized to the active `spiffs` partition), and emit a `flash-manifest.json` plus a merged `.unified.bin`.
  - Added robust discovery for core-provided utilities (`mklittlefs`, `esptool`), supporting both Python-script and native binary variants shipped with ESP32 core 3.x.
  - Updated `tools/arduino-upload.sh` to consume the manifest and flash all segments (bootloader, partitions, app, LittleFS) in one `esptool` pass; defaults to rebuilding unless `--no-build` is specified.
  - Documented the new workflow in `docs/BUILD.md`; upload helper now keeps firmware + filesystem assets in sync by design.
Prompt: can we please allow for two fans? both are side by side pushing air through the chassis so independent control is probably not super important, but i'd imagine we'd want them both on separate pins
- Dual-fan support
  - Expanded `config.h` to define two PWM outputs/channels (`PIN_FAN1_CTRL`=`25`, `PIN_FAN2_CTRL`=`26`) with opt-out via `-1`, and exposed shared arrays for iteration.
  - Updated firmware (`bootbox_mcu_fw.ino`) to initialise/drive both fans in parallel, report the active fan count via WS/HTTP state, and log the number of fans found at boot.
  - Enhanced the dashboard (`index.html`/`app.js`) to surface the fan count in the mode badge and keep UX messaging consistent after controller sync.
  - Rebuilt + reflashed via the automated scripts (`tools/arduino-build.sh`, `tools/arduino-upload.sh --no-build`) and verified `/api/state` now reports `"fan_count": 2` with the refreshed UI served from LittleFS.
Prompt: great! lets now start work on implementing the ADAU1701 features, beginning with a modular framework for updating and reading parameters via knob, slider, vertical 'fader',  etc. type elements with a 'display' reflecting the value above. the 'display' can be clicked on and typed into to set precise values. I want to maintain the live, responsive nature of the project such that turning a knob on the ui 'feels like' turning a real knob in an analog system. ...
- DSP control scaffold
  - Extended `Settings`/NVS persistence with ADAU1701 placeholder parameters (master/stereo/sub gains, crossover pair, per-band HP/LP) and added a deferred NVS flush to reduce flash churn.
  - WebSocket/HTTP state now advertises a `dsp` payload; new `set_dsp` handler clamps inputs, maintains crossover linkage, schedules persistence, and logs updates.
  - Rebuilt the dashboard with live knobs, sliders, and vertical faders driven by a reusable JS control layer. Displays are editable for precise entry, updates are throttled but stay responsive, and the fan badge reflects active fan count + mode.
  - Added crossover link toggle and sub-band range enforcement on both firmware and UI, plus batched WebSocket updates for smooth "analog" interaction.
- Follow-up tweaks: DSP card expands full-width on wide viewports, knob drag now disables transition during interaction, and redundant state echoes are ignored to prevent the "snap back" visual glitch on release. Thermal inputs now show applied vs pending values so edits no longer revert prior to saving.
- Added a lightweight tabbed layout (Cooling / DSP / System) with collapsible cards so the dashboard stays focused on the task at hand.
Prompt: let's also remove any references to 'ESP32', keep any branding MCU agnostic.
- Firmware/UI polish
  - Extended `/api/state` and WebSocket broadcasts with a `sys` payload (uptime, heap, LittleFS usage, firmware build metadata, connected clients) and scrubbed chip identifiers to avoid explicit ESP32 branding.
  - Refreshed the System tab with a summary card that renders the new diagnostics, added uptime/byte formatting helpers, and persisted both tab selection and card collapse state via `localStorage`.
  - Updated dashboard subtitle, README, and build notes to use MCU-agnostic wording while keeping existing command examples; no user-facing copy mentions "ESP32".
- Verification
  - Rebuilt and reflashed unified firmware + filesystem (`tools/arduino-build.sh --config-file arduino-cli.yaml`, `tools/arduino-upload.sh --no-build --config-file arduino-cli.yaml`).
  - Confirmed `sys` data over HTTP (`curl http://192.168.4.1/api/state`) and via a Python `websockets` client (request `get_state`, receive `state`, validate keys, send ack). UI assets fetched from LittleFS show the new System Summary markup.
Prompt: looking good, except i am still seeing the issue with updates clearing pending settings not yet saved in a few places, particularly the pid/manual mode setting.
- Pending state retention fixes
  - Added explicit dirty tracking for mode toggles and the manual fan slider in `data/app.js`, preventing periodic `state` broadcasts from overwriting unsaved selections. Mode badges now refresh fan counts without clobbering user-picked modes, and manual slider labels display a `(pending)` suffix until settings are persisted.
  - Gate controller-driven UI refreshes on dirty flags, preserve unsaved thermal edits via a helper, and soften status-line updates so "remember to save" messages persist until the user commits or reverts.
  - Reset dirty markers on save, manual restoration, or when incoming state matches the pending value; slider/tooltips automatically clear pending cues when values converge.
- Validation
  - Rebuilt and reflashed (`tools/arduino-build.sh --config-file arduino-cli.yaml`; `PORT=/dev/ttyUSB0 tools/arduino-upload.sh --no-build --config-file arduino-cli.yaml`).
  - Exercised REST/WS paths: `curl http://192.168.4.1/api/state` and a Python `websockets` smoke (toggle `set_settings` between PID/Manual, confirm transitions and reliable acks). Pending UI regression verified via code inspection; ready for interactive browser confirmation.
Prompt: i notice that the uptime occasionally resets back to 0s, usually accompanied by a 'reconnecting' message - this seems to imply something is causing the MCU to reset?
- Reset instrumentation & diagnostics
  - Cached static MCU metadata (chip label, flash totals) and added `reset_reason`/`boot_count` tracking so `/api/state` + dashboard can reveal real resets versus transient Wi-Fi drops. These values now appear in the System tab alongside logs, and new boots log `reset reason` + incrementing counter.
  - Added helper to map `esp_reset_reason()` and persisted the boot counter in Preferences; logs ring buffer shows the reason sequence for historical context.
  - Rebuilt + reflashed (`tools/arduino-build.sh --config-file arduino-cli.yaml`; `PORT=/dev/ttyUSB0 tools/arduino-upload.sh --no-build --config-file arduino-cli.yaml`), then verified telemetry via `curl http://192.168.4.1/api/state | jq '.sys'` and `/api/logs`.
- WS stability hardening
  - Observed recurring panic traces bubbling up from `AsyncWebSocketClient::canSend()` after clients disconnected. Removed the custom resend queue entirely and now rely on the library-level delivery; server still emits `id` fields for optional acks, but no longer resends on our own.
  - Rebuilt firmware (`tools/arduino-build.sh --config-file arduino-cli.yaml`). Upload pending once port access frees up.
Prompt: so lets make a few tweaks before committing this version and harnessing the board up - can we get the onboard LED not to stay on continuously...
- Status LED patterns
  - Wired GPIO2 (builtin LED) into the firmware with reusable pattern definitions for Boot, System OK, Check Logs, General/Thermal/Network errors, plus placeholder Bluetooth/DSP states. Patterns use distinctive Morse-style flashes per requirement.
  - Status is derived from boot diagnostics (reset reason, storage/AP availability) with dynamic overrides for thermal alarms; future DSP/Bluetooth hooks can call `setLedStatus` once those subsystems land.
  - Added open-loop helper to tick the LED without blocking and documented defaults (System OK steady flash, panic resets drive “Check Logs”, thermal faults override with triple-pulse).
Prompt: still not quite right - for probe 2, the dashboard is giving 34.8c but my IR therm reads 18.4c
- firmware/arduino/bootbox_mcu_fw/config.h
  - Updated channel 2 thermistor configuration to reflect the ~180 kΩ series resistor paired with the 100 kΩ probe, yielding accurate ambient conversion.
  - Added optional thermistor debug logging constants to keep calibration prints behind a compile-time switch.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Wrapped periodic Serial calibration prints behind the new debug guard so production builds stay quiet while keeping the helper available.
- Build & verification
  - Rebuilt unified firmware + SPIFFS image, flashed to `/dev/ttyUSB0`, and confirmed `/api/state` now reports `temp2≈21.7°C` for the measured 20.8°C probe.
Prompt: is there a way to add a thermistor calibration utility into the system page? perhaps a utility which approximates the temp curve by guiding the user to hold the thermistor to be calibrated at a known high temperature, then a known low temp? say, boiling water, and refrigerated water? the curve can then be stored to NVS? also, I note in this latest build that the status LED is no longer lighting. if we can also disable the temperature debug serial logging for now too.
- firmware/arduino/bootbox_mcu_fw/config.h
  - Added status LED pin/polarity configuration and documented defaults for the onboard indicator.
  - Kept thermistor debug logging disabled by default for quiet serial output.
- firmware/arduino/bootbox_mcu_fw/settings.h
  - Persist thermistor calibration data (nominal resistance, beta, valid flag) for both channels in NVS.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Switched Status LED logic to use configurable polarity, refreshed thermistor runtime parameters, and gated debug logs.
  - Implemented thermistor calibration workflow with averaged ADC sampling, two-point curve solve, NVS persistence, and AsyncJSON HTTP endpoints.
  - Extended system telemetry payloads with thermistor metadata and active calibration session status.
- firmware/arduino/bootbox_mcu_fw/data/index.html
  - Added a System-tab calibration card with channel selector, capture controls, and a summary table.
  - Introduced supporting layout/styles for calibration workflow messaging and table output.
- firmware/arduino/bootbox_mcu_fw/data/app.js
  - Wired new calibration UI: state tracking, button enablement, API orchestration, and table/session rendering.
  - Fetched calibration status on load and integrated thermistor data into the existing system telemetry updater.
- Build & verification
  - `./tools/arduino-build.sh --config-file arduino-cli.yaml`
  - `PORT=/dev/ttyUSB0 ./tools/arduino-upload.sh --no-build --config-file arduino-cli.yaml`
  - `curl http://192.168.4.1/api/state` and exercised `/api/therm/calibration` actions (start/capture/cancel) to confirm firmware/UI state updates.
Prompt: can you please commit everything to main on github and round out the documentation making sure it is all up to date, including starting the Wiki on github with at least stubs for all topics we intend to cover on there. please upload an archive to releases on github as release 0.1, then we will get into building out the ADAU DSP interaction feature set in earnest.
- README.md
  - Documented thermistor calibration workflow, status LED codes, updated feature list/HTTP endpoints, and added release packaging guidance.
- docs/BUILD.md
  - Added CP2102 note, thermistor calibration reminder, and a step-by-step release checklist including `gh release create` usage.
- docs/wiki/
  - Seeded wiki stub pages (Home, Hardware, Firmware, Web-UI, DSP Control roadmap, Thermistor calibration, Build & Flash, Release process, Troubleshooting) ready to sync once GitHub wiki cloning is permitted.
- firmware/arduino/bootbox_mcu_fw/config.h
  - Confirmed 3-wire fan config and documented dual 10k NTC parameters for the new hardware.
- firmware/arduino/bootbox_mcu_fw/settings.h
  - (Previously added) thermistor calibration persistence retained; no additional delta beyond staged work.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - (Previously added) thermistor calibration backend retained; verified with new config.
- firmware/arduino/bootbox_mcu_fw/data/index.html & data/app.js
  - (Previously added) calibration UI kept intact; no extra functional edits beyond doc alignment.
- docs/wiki note
  - Unable to push to `bootbox-mcu-fw.wiki.git` (GitHub still returns 404) so included markdown locally and documented the blocker for follow-up.
- Release artifacts
  - Rebuilt with `tools/arduino-build.sh --config-file arduino-cli.yaml` ready for packaging/upload after commit.
Prompt: okay, lets build out the DSP management feature set...
- firmware/arduino/bootbox_mcu_fw/settings.h
  - Removed legacy fixed DSP fields from the settings struct/load/save helpers so the new dynamic system owns persistence.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Added a full DSP manager: bundle discovery, XML schema parsing, preset storage, debounced value persistence, new `/api/dsp/*` endpoints, and WebSocket/state plumbing.
  - Reworked `/api/upload/adau` to target bundle folders (`program.bin`/`interface.xml`) and wired the new `/api/dsp/action` handler for select/rename/delete/push + preset operations.
  - Broadcast the new DSP payload in `/api/state`/WS frames and dropped the static `Settings` fields/handlers.
- firmware/arduino/bootbox_mcu_fw/data/index.html
  - Replaced the static DSP mixer with a bundle manager + dynamic controls card, preset chips, and upload UI elements.
  - Added structural styles for bundle rows, dynamic control cards, preset chips, and status messaging.
- firmware/arduino/bootbox_mcu_fw/data/app.js
  - Rebuilt the DSP front-end: dynamic schema rendering, bundle/preset management, REST helpers, upload flow, and WebSocket sync with the new backend contract.
  - Removed the old hard-coded knob/slider logic, dirty queues, and cross-link toggles; added new state objects and status handling.
- README.md
  - Documented the DSP bundle workflow, new endpoints, and interface XML format alongside the updated feature list.
- docs/wiki/DSP-Control.md & docs/wiki/Web-UI.md
  - Updated wiki stubs to describe the bundle/schema/preset system and the UI flows.
Prompt: lets get that going, im keen to get the two boards integrated
- firmware/arduino/bootbox_mcu_fw/config.h
  - Added ADAU/I²C configuration knobs (SDA/SCL pins, I²C clock, codec + EEPROM addresses, optional reset pin) so the transport can be tailored to each harness.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Integrated Wire/I²C, added an ADAU manager (EEPROM writer, reset pulser, 5.23 conversion, parameter register writes) and tied it into DSP bundle pushes and live control updates.
  - Bundles now push `program.bin` into the ADAU EEPROM and pulse reset to keep the self-boot workflow; dynamic controls stream to hardware via the XML-mapped addresses/formats.
  - Exposed hardware status/error info via `/api/dsp/schema`, applied DSP values on preset load/startup, and refreshed system telemetry accordingly.
- firmware/arduino/bootbox_mcu_fw/data/index.html
  - Rebuilt the DSP tab UI into a bundle/preset manager with status line and dynamic control grid matching the uploaded schema.
- firmware/arduino/bootbox_mcu_fw/data/app.js
  - Added REST helpers, bundle/preset management, dynamic control rendering, status messaging, and direct WebSocket updates for the new DSP endpoints; removed the fixed mixer widgets.
- README.md / docs/wiki/DSP-Control.md / docs/wiki/Web-UI.md
  - Documented the ADAU transport, updated the bundle/preset workflow, interface schema semantics, and UI behavior. Synced the GitHub wiki with the new content.
- Build
  - `./tools/arduino-build.sh --config-file arduino-cli.yaml`
Prompt: is the communication layer to/from the ADAU set up yet? lets get that going, im keen to get the two boards integrated
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Added ADAU hardware status fields to `/api/dsp/schema`, improved `push_bundle`/preset responses, and surfaced controller errors back to the client.
  - Reworked `applyPreset` to return descriptive HTTP errors, keep local values in sync, and only fail when I²C writes actually break.
  - Treated UI-only controls (`address == 0`) as successes and emit "ADAU link offline" when the transport is down so callers see a concrete reason.
- firmware/arduino/bootbox_mcu_fw/data/app.js
  - Sanitized control specs client-side, added clamping helpers, and prevented malformed schema data from breaking slider/toggle rendering.
  - Introduced a DSP hardware status line that consumes `hw_ready`/`hw_error`, refreshes after pushes/schema loads, and mirrors log errors inline.
  - Improved control initialization/value updates (default positions, toggle labels, clamped WebSocket writes) and ensured post-push refreshes pull the latest schema.
- firmware/arduino/bootbox_mcu_fw/data/index.html
  - Added the DSP hardware status placeholder beneath the controls card for the new UI indicator.
- README.md
  - Documented the `hw_ready`/`hw_error` telemetry and clarified the `/api/dsp/schema` response fields.
- docs/wiki/DSP-Control.md
  - Noted that the schema endpoint now surfaces `schema_ready`/`hw_ready`/`hw_error` for automation and UI consumers.
- docs/wiki/Web-UI.md
  - Updated the DSP tab description to call out the new status line and detailed ADAU error messaging.
- Build & verification
  - Ran `./tools/arduino-build.sh --config-file arduino-cli.yaml` to regenerate the firmware + LittleFS/SPIFS/unified artifacts with the updated DSP flow.
Prompt: please now put some good effort into detailed documentation on the wiki including how-to articles where relevant, and information on the bundle schema and generating them. include examples. if you realise in the process that any changes ought to be made to the firmware for function, stability or clarity, please do so.
- docs/wiki/Home.md
  - Added navigation overview, split reference vs how-to sections, and linked in the new DSP bundle guide.
- docs/wiki/DSP-Bundle-How-To.md
  - Created a full how-to covering SigmaStudio export, interface authoring, upload (UI + curl), push/preset workflow, and troubleshooting tips.
- docs/wiki/Build-and-Flash.md
  - Expanded toolchain description, detailed artifact outputs, flashing alternatives, and post-flash verification checklist.
- docs/wiki/Firmware.md
  - Documented runtime architecture, ADAU manager behavior, persistence layout, and major modules.
- docs/wiki/Hardware.md
  - Added platform targets, wiring tables, ADAU notes, and EMI/cabling recommendations.
- docs/wiki/Web-UI.md
  - Described tab behaviors, WS/dirty-state flow, accessibility notes, and best practices per tab.
- docs/wiki/DSP-Control.md
  - Extended schema reference with attribute tables, REST API summary, preset format, and automation guidance.
- docs/wiki/Thermistor-Calibration.md
  - Rewrote workflow with detailed UI steps, API payload examples, and practical tips.
- docs/wiki/Release-Process.md
  - Added pre-flight checks, packaging commands, tagging instructions, and post-release tasks.
- docs/wiki/Troubleshooting.md
  - Converted to a table-driven cheat sheet with additional DSP/network tips and serial debugging guidance.
- README.md
  - Linked to the expanded wiki, highlighting where to find bundle, build, and calibration guides.
Prompt: check out where we're at and familiarise yourself with the code and ethos. going to be working on the firmware today and cleaning it up, checking for bugs, and doing any review necessary to make the codebase dual-target, building and optimising for ESP32-S3 (devkit version, 16M Flash, 8M PSRAM).
- README.md / docs/wiki/*
  - Reviewed the published architecture, hardware assumptions, and workflow expectations so upcoming firmware changes stay aligned with the documented ethos.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino, config.h, settings.h
  - Skimmed runtime modules (Wi-Fi/AP stack, DSP manager, thermal control, fan PWM, status LED) and configuration hooks to map out potential ESP32-S3 deltas; no code edits yet.
- tools/arduino-*.sh
  - Read the build/upload/library helper scripts to understand how FQBN, partition data, and LittleFS assets are derived, informing the dual-target plan.
Prompt: get into it! take initiative, do your best work, and remember this thing needs to be rock-solid and user-proof -- so keep UX and reliability front of mind!
- firmware/arduino/bootbox_mcu_fw/config.h
  - Added automatic ESP32 vs ESP32-S3 detection (via `CONFIG_IDF_TARGET` with manual override hooks) and introduced target-specific pin defaults for fans, thermistors, I²C, and status LED polarity so both devkits work without editing the file.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Included psram helpers, guarded Status LED initialization when no pin is defined, hardened thermistor sampling for disabled pins, and expanded `/api/state` with PSRAM + IDF target telemetry for better UX diagnostics.
- tools/arduino-build.sh / tools/arduino-upload.sh
  - Taught the build script to capture the chip type from `sdkconfig`, propagate it through `flash-manifest.json`, and use it when merging binaries; the uploader now respects that metadata so `esptool` flashes ESP32 or ESP32-S3 correctly.
- README.md / docs/BUILD.md / docs/wiki/*
  - Documented the dual-target workflow (FQBN strings, build/upload examples, release packaging expectations) plus new hardware tables, troubleshooting tips, and UI callouts for the additional system telemetry.
- Builds
  - Verified both targets compile by running `tools/arduino-build.sh --fqbn esp32:esp32:esp32` and `... --fqbn 'esp32:esp32:esp32s3:FlashSize=16M,PSRAM=opi,USBMode=hwcdc,PartitionScheme=default_8MB' --build-path .arduino-build-s3`.
Prompt: can you please update the build tooling:- set it up so we can choose via flags which target 'definition' we are targeting, which should take the form of .def files inside a particular relative directory, or specified explicitly via flag. each target has one .def file defining all relevant characteristics of the system, or to put it another way, it describes all of the ways it can differ from other boards, such that the build system has all the information it needs to compile the binaries, create the filesystem and partitions (which utilise full storage capacity as defined in the .def file), enable/configure board-specific build flags such as PSRAM utilisation, and flash a connected board (def file contains the /dev/tty* path and uids necessary to confirm board location and identity[if desired]). The idea is to create a build script/system that is flexible and easy to reconfigure, but also very fast to use. As such, could you create the described tooling/script/s and .def files for both the ESP32-devkitC (esp32:esp32:esp32) and the ESP32-S3 (esp32s3:esp32s3:esp32s3), with 16M flash and 8M PSRAM. hoping to see this reflected in the web interface once the new build system has compiled and flashed.
- targets/
  - Added per-board manifests (`esp32-devkitc.def`, `esp32s3-devkitc.def`) plus custom partition CSVs sized to use 100 % of the flash for each device. Definitions capture FQBN overrides, flash size validation, upload port identity metadata, and dedicated build folders.
- tools/target-build.sh
  - New helper that ingests a `.def` file, validates the partition layout against the declared flash capacity, copies the partition CSV into the sketch for the build, and optionally flashes the board after verifying its VID/PID/serial via `udevadm`. Supports `--flash`, `--flash-only`, `--list`, and `--skip-identity`.
- README.md / docs/BUILD.md / docs/wiki/Build-and-Flash.md / docs/wiki/Release-Process.md / docs/wiki/Troubleshooting.md
  - Documented the target-definition workflow, updated command examples to use the new custom partition scheme, and described how to extend or list the available targets.
- Build & flash
  - Ran `tools/target-build.sh --target esp32s3-devkitc --flash` to regenerate the firmware/LittleFS image with the 16MB partition table and deploy it to `/dev/ttyACM0`, verifying the new tooling end-to-end.
Prompt: can you please add more robust handling of invalid/not-present thermistor conditions? I don't wan't the cooling/pid system to enter an extreme state or think that a temperature in the hundreds of degrees, or below -20c is a valid sensor input
- firmware/arduino/bootbox_mcu_fw/config.h
  - Introduced `THERM_VALID_MIN_C`/`THERM_VALID_MAX_C` so the acceptable range is centralized alongside the other thermal knobs.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Added a sanitization helper that converts out-of-range readings to `NaN`, rate-limits a log entry per channel, and prevents the PID loop from reacting to floating/broken probes.
- README.md / docs/wiki/Hardware.md
  - Documented the new guardrail so users know out-of-bounds temperatures are ignored automatically.
Prompt: please add an interactive mode for the build tooling if invoked without valid args...
- tools/target-build.sh
  - Rebuilt the helper around an interactive core: when run without CLI flags it now presents a menu to select/edit/deduplicate/delete `.def` files, tweak serial/baud/strict validation overrides, and trigger build/flash operations. Added safety prompts that can generate a new `.def` using the connected board’s VID/PID/serial if strict validation fails.
- tools/target-build.sh
  - Corrected the definition enumerator to read every `.def` in the targets directory (using `mapfile`), so both ESP32 and ESP32-S3 manifests show up in the interactive manager/`--list` output.
- README.md / docs/BUILD.md / docs/wiki/Build-and-Flash.md
  - Documented the interactive workflow and how to launch it, alongside the existing CLI examples.
- tools/target-build.sh
  - Added a `cls()` helper plus fail-safe prompting so interactive sessions clear the screen per menu, guard against missing `flash-manifest.json`, and fall back to the menu with corrective actions instead of exiting when builds or uploads fail.
- tools/target-build.sh
  - Added screen clearing to all interactive menus (main dashboard, definition selector, overrides dialog, definition manager) so the UI refreshes cleanly as you navigate.
- tools/target-build.sh
  - Enhanced the interactive flow to keep users inside the menu: status messages persist across clears, build/flash actions now pause (success or error) instead of exiting, and missing artifacts (e.g., `flash-manifest.json`) trigger guided fixes or automatic rebuilds before flashing.
Prompt: please familiarise yourself with the project, some establishing notes are available in the journal.txt file, and much of the ethos can be gleaned from the documentation. then please look more specifically at the implementation. Another agent has just made some changes to the bootbox code to integrate it with a second ESP32 board -- I want you to validate his work and ensure the system is consistent and consideration for stability is made, specifically to communications between the three-device network. (esp32-wroom, esp32-S3, ADAU1701).
- firmware/arduino/bootbox_mcu_fw/config.h
  - Reviewed new Bluetooth link pin/baud defaults and target-specific mappings to understand UART wiring expectations.
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Reviewed bt2i2s UART link handling, WS/HTTP surfaces, heartbeat/timeout logic, and state propagation for stability assessment.
- firmware/arduino/bootbox_mcu_fw/data/app.js
  - Reviewed System tab Bluetooth card behavior, WS retry adjustments, and bt_cmd dispatch flow.
- firmware/arduino/bootbox_mcu_fw/data/index.html
  - Reviewed UI layout additions for the Bluetooth link card and controls. No code changes made.
Prompt: please address these issues holistically and thoroughly, and any possible bugs or concerns you may find in the process. work across both bootbox AND bt2i2s codebases to ensure consistency of the system. Please keep in mind the end user...
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Hardened bt-link handling: fixed hello parsing (fw vs version), added link-ready gating for WS/HTTP BT commands, cleared stale metadata/state on timeout, tracked last_seen age, and parsed new bt_devices frames. Added device/pairing state surfaces, WS/API endpoints for device list, and retry-less error responses when UART is unavailable. Increased JSON buffers for larger payloads and added device request polling for proto>=2.
- firmware/arduino/bootbox_mcu_fw/config.h
  - Expanded BT buffer sizes to match richer link frames.
- firmware/arduino/bootbox_mcu_fw/data/app.js
  - Updated BT state handling to use link-reported age instead of client timestamps, added device/pairing management (list, reorder, connect/forget, pairing start/stop), and disabled controls when the link is offline/unsupported. Adjusted bt_cmd payloads and UI rendering to surface new metadata.
- firmware/arduino/bootbox_mcu_fw/data/index.html
  - Added paired-device UI elements (list, priority controls, pairing buttons) and styling for the Bluetooth card.
- /home/sam/Documents/bt2i2s/firmware/arduino/bt2i2s/bt2i2s.ino
  - Extended link protocol to v2 with paired-device management: added Preferences-backed registry, bonded-device sync, connect/forget/priority/pairing commands, pairing mode with timeout, and autoconnect to top-priority device. Included peer metadata in state frames and device lists, cleared stale state on drop, and exposed hello/cmd features list. Added build guards for legacy I2S API and gap scan-mode handling compatible with esp32 core 3.3.x.
- Builds
  - Bootbox firmware: `arduino-cli compile --config-file arduino-cli.yaml --fqbn esp32:esp32:esp32 firmware/arduino/bootbox_mcu_fw`
  - bt2i2s firmware: compiled against bootbox toolchain/libs via `arduino-cli compile --config-file ../bootbox-mcu-fw/arduino-cli.yaml --fqbn esp32:esp32:esp32 --libraries firmware/arduino/libraries firmware/arduino/bt2i2s` (local bt2i2s arduino data lacks esp32 core; initial install attempt timed out on the 428MB esp32 libs download).
Prompt: please now thoroughly comb through the bootbox code specifically... ensure ESP32-S3 features/pins are appropriate
- Review only: audited Bootbox firmware/config/docs for ESP32-S3 targeting, checked pin assignments (UART12/13 link, I2C 8/9, therm 4/5, fans 16/17, LED_BUILTIN) against DevKitC-1 availability/strap pins, confirmed no conflicts; noted the onboard RGB LED needs a proper WS2812 driver (StatusLed will not light it). No code changes made.
Prompt: Please also flesh out and complete the implementation of the 4-wire fan tachometer feedback...
- firmware/arduino/bootbox_mcu_fw/bootbox_mcu_fw.ino
  - Implemented 4-wire fan tach counting (interrupt-driven), RPM calculation, and state reporting; gated tach data to 4-wire mode only and kept 3-wire as default. Added per-fan RPM plus representative RPM in API/WS state and initialized tach hardware during setup.
Prompt: consolidate bt2i2s into bootbox repo and keep link compatibility in sync
- firmware/arduino/bt2i2s
  - Imported the bt2i2s firmware (A2DP/AVRCP sink + UART link) into the repo for side-by-side development with Bootbox.
- firmware/arduino/libraries/ESP32-A2DP
  - Vendored the ESP32-A2DP library to support the bt2i2s build from this repo.
- README.md / docs/Link-Compatibility.md
  - Documented the dual-firmware layout, build entry points, S3 tach pins, and added a link-compatibility checklist to keep Bootbox/bt2i2s changes in lockstep.
